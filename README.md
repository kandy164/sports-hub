Student Information
Student Name: Emmanuel
Student ID: 2508209496
GitHub Repository URL: https://github.com/kandy164/sports-hub

Question 1: Website Creation
I created a Sports News and Analysis Website using only HTML. The website contains the following sections:

Latest News – Transfer updates and match results

Upcoming Fixtures – Presented in table format

Match Analysis – A detailed preview of the Lens vs PSG match

Player Spotlight – Featuring Khvicha Kvaratskhelia

Contact Form – Allows users to submit information through the website

HTML Elements Used (25+):
!DOCTYPE,html,head,title,meta,body,header,nav,main,section,article,aside,footer,h1–h3,p,a,ul,li,table,tr,th,td,form,input,textarea, button,figure,figcaption,hr,strong.

HTML Attributes Used (15+):
lang, charset, name, content, keywords, author, viewport, title, style, id, href, src, alt, width, height, border, action, method, placeholder, rows, cols, type

Question 2: HTML Elements
Most challenging elements:

form – structuring the inputs and labels correctly

table – aligning headers and rows correctly for the upcoming fixtures

aside – deciding which additional information was appropriate to place in the sidebar

figure and figcaption – applying semantic HTML correctly to the player spotlight image and its caption

nav – organizing the navigation links clearly and logically

Use of semantic elements:

header → site title and navigation

section → grouped content (Latest News, Fixtures, Analysis, Contact)

article → individual news items, match previews, player spotlight

footer → copyright and back-to-top link

Most useful element for layout:
section because it allowed clear division of content by topic.

Question 3: HTML Attributes
Three essential attributes:

href – used for navigation links

src – used to specify the source of the player image

type – used to define the type of form inputs

Class vs ID usage:

id → unique identifiers for sections (e.g., id="analysis")

class → used for grouping multiple elements for styling (not heavily applied here since no CSS, but useful if extended later)

Attribute that improved user experience most:
alt → provides alternative text for images, improving accessibility.

Question 4: Development Process
Planning:
Sketched layout → decided sections (News, Fixtures, Analysis, Player Spotlight, Contact) → listed required elements.

Testing and Debugging:
Opened HTML in browser → used Inspect tool → validated with W3C Validator.

Challenges and Solutions:

Long analysis text → solved by breaking into short p blocks with bold highlights

Match results formatting → solved by using ul for clarity

Question 5: Git & GitHub Implementation
Git commands used:
git init
git add .
git commit -m "message"
git push origin main

Commit strategy:
Around 12 commits with descriptive messages like:

Added Latest News section

Inserted Lens vs PSG analysis

Updated Player Spotlight

Importance of version control:
Tracks changes, allows rollback, supports collaboration, and keeps project history.

Question 6: Code Quality & Best Practices
Ensuring valid HTML:
Used W3C Validator, checked browser console for errors.

Best practices followed:

Semantic tags

Proper indentation

Accessibility attributes (alt)

Meaningful commit messages

Organized content into logical sections

Improvements with more time:

Adding CSS styling for professional look

Responsive design for mobile

JavaScript for interactivity
